class Note {
  String noteId;
  String noteTitle;
  DateTime createdDateTime;
  DateTime lastEditDateTime;

  Note(
      {this.noteId,
      this.noteTitle,
      this.createdDateTime,
      this.lastEditDateTime});
}
